# logrinto Logo generator

download the repository
```sh
$ git clone
```

install npm
```sh
$ npm install
```

start the server
```sh
$ npm run start
```

The generator is running on:
http://localhost:3000/
